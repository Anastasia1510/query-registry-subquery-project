{
  "name": "subquery-query-registry-project",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Subquery",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^6",
    "@subql/cli": "latest",
    "@subql/types": "latest",
    "@types/bs58": "^4.0.1",
    "moonbeam-types-bundle": "^2.0.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@subql/contract-processors": "^0.3.2-1",
    "@subql/contract-sdk": "0.1.1-1",
    "bs58": "^4.0.1"
  },
  "resolutions": {
    "class-transformer": "0.4.0"
  }
}
